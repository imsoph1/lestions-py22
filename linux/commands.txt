MacOs(B спотлайт вводим Терминал)
~ - символ домашней директории(папки)
/ - символ корневой директории(папки)


.. - обозначает предыдущую директорию
. - обозначает текущую директорию

COMMAND+C - скопировать 
COMMAND+V - втавить

1. ls(list) -> Выводит список всех директории файлов в текущей директории(рабочая директория)

ls - a -> выводит список как и скрытых так и обычных файлов и директории 

ls - l -> выводит список директорий и файлов с подборной информацией 
ls - t -> выводит список как и скрытых так и обычных файлов с подробной информацией 
2. cd(change directory) -> меняет текущую директорию на указанную 
cd <название директории>


3. mkdir(make directory) -> Создает указанную директорию
mkdir <директория>

4. touch -> создает указаный файл
touch <name_of_file>.<extension(расширение(txt, docs))>

5. pwd(print working directory) -> выводит путь до текущей директории

6. rmdir (remove directory) -> удаляет указанную директорию (не может удалить не пустую директорию)

7. rm -> удаление файлов 
rm <name_of_file>
